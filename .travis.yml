sudo: required
branches:
  only:
    - dev
language:
    - cpp
compiler:
    - gcc
jobs:
  include:
    - stage: cmake
      script:
      - cmake .
      - make
      - ./CMake-test
      - ctest
    - stage: ctest
      script:
      - ctest
    - stage: Second method
      script:
      - mkdir _builds
      - cd _builds
      - cmake ..
      - make
      - ctest
notifications:
   #email: false
   email:
     on_success: never # default: change
     on_failure: always # default: always
